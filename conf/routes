# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~
GET         /rooms        controllers.Conference.getRooms
POST        /rooms        controllers.Conference.addRoom

GET         /talks        controllers.Conference.getTalks
POST        /talks        controllers.Conference.addTalk
